# wmi

Windows Management Instrumentation (WMI) operations.

---

## Usage

```
r4t wmi [flags]
```

> **Status**: Base command registered. Subcommands are not yet implemented.

WMI support is planned for remote system querying and command execution via DCOM/WMI (port 135 + dynamic RPC ports). When implemented, it will use the global authentication flags.
